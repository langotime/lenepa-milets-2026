This repository is the cleaned LeNEPA MiLeTS 2026 reproduction repo.

Do not commit raw datasets, checkpoints, W&B run directories, Hydra outputs, VM logs, or local training outputs.

Historical job scripts under `experiments/legacy_job_scripts/` are provenance copies; keep their old paths intact unless explicitly preparing runnable local variants.

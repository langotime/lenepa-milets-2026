# Experiment Manifests

`main_matrix.csv` is the paper-facing summary of the PTB-XL/Aionoscope experiment matrix.

`cauker_ucr.yaml` captures the single CauKer2M to UCR run used for the frozen-encoder check.
